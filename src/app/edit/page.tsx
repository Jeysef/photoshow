import Page from "@/components/pages/edit/page";
import type { Metadata } from "next/types";

export default Page;

export const metadata: Metadata = {
    alternates: {
        canonical: "/edit",
    },
    description: "Let us create a video for you!",
};
